# NOC Toolkit

**Версия:** 0.6.1
**Единый инструментарий для NOC операций — CLI и GUI**

---

## 📖 Обзор

NOC Toolkit предоставляет простой доступ к различным операционным инструментам, используемым командой NOC. Доступны два интерфейса:

- **CLI** (`noc-toolkit.py`) — интерактивное меню в терминале
- **GUI** (`noc_toolkit_gui.py`) — десктоп-приложение с тёмной темой, параллельным запуском инструментов, логами и интерактивными параметрами

Вместо того чтобы запоминать расположение множества скриптов и команд, просто запустите toolkit и выберите нужный инструмент.

---

## ✨ Возможности

- 🎯 **Единый интерфейс** — CLI + GUI для всех NOC инструментов
- 🖥️ **Десктоп GUI** — Тёмная тема на customtkinter с вертикальными вкладками
- ⚡ **Параллельный запуск** — Одновременное выполнение нескольких инструментов в GUI
- 📋 **Интерактивное меню** — Простая навигация через CLI меню
- 📝 **Логи по инструментам** — Каждый запуск сохраняется в `logs/` с таймстемпом
- 🔧 **10 инструментов** — На данный момент включает:
  - **PD Sync** — Синхронизация инцидентов PagerDuty с Jira
  - **PD Jobs** — Извлечение названий джоб из PagerDuty
  - **PD Monitor** — Мониторинг и авто-подтверждение triggered инцидентов
  - **PD Merge** — Поиск и слияние связанных инцидентов по имени джобы
  - **Freshness** — Отчёт DACSCAN о свежести данных через Databricks SQL
  - **Shift Report** — Синхронизация Jira статусов в shift report (Google Sheets / Excel)
  - **GSheet Report** — Google Sheets адаптер для Shift Report
  - **PD Escalate** — Автоматизация эскалации после DSSD
  - **PD Resolve** — Авто-resolve PD инцидентов при восстановлении Airflow
  - **Ticket Watch** — Мониторинг тикетов на неназначенные/застойные состояния
- 🔐 **Централизованная конфигурация** — Настройте один раз, используйте везде
- 🚀 **Расширяемость** — Легко добавлять новые инструменты
- ✅ **Проверка здоровья** — Автоматически проверяет доступность инструментов
- 🧪 **1275 юнит-тестов** — 94% покрытие кода по всем модулям

---

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.10 или выше
- pip (менеджер пакетов Python)

### Установка

1. **Перейдите в директорию toolkit:**
   ```bash
   cd /path/to/noc-toolkit
   ```

2. **Установите зависимости:**
   ```bash
   pip3 install -r requirements.txt
   ```

3. **Настройте конфигурацию:**

   ```bash
   cp config.yaml.example config.yaml
   # Отредактируйте config.yaml и заполните ваши учетные данные
   ```

   Или используйте `.env` файл — см. документацию конкретного инструмента.

### Запуск

**CLI режим** — интерактивное меню в терминале:

```bash
python3 noc-toolkit.py
```

**GUI режим** — десктоп-приложение:

```bash
python3 noc_toolkit_gui.py
```

> **Примечание:** GUI требует `customtkinter` и опционально `pyobjc-framework-Cocoa` (macOS) для плавной прокрутки мышью/трекпадом. Оба включены в `requirements.txt`.

---

## 🖥️ GUI Режим

GUI (`noc_toolkit_gui.py`) — современный десктоп-интерфейс с тёмной темой на customtkinter.

### Ключевые возможности

- **Вертикальный сайдбар** — Вкладки инструментов слева для быстрой навигации
- **Параметры по инструментам** — У каждого инструмента своя панель с чекбоксами, радио-кнопками и полями ввода
- **Параллельный запуск** — Запускайте несколько инструментов одновременно; каждый в своём подпроцессе
- **Консоль по инструментам** — Переключайтесь между вкладками для просмотра вывода каждого инструмента
- **Лог-файлы по инструментам** — Каждый запуск сохраняется в `logs/<tool_id>_<timestamp>.log`
- **Панель конфигурации** — Доступна по кнопке в шапке; редактирование `config.yaml` (API токены, URL и т.д.)
- **Разделитель** — Перетаскиваемый сплиттер между панелью параметров и консолью
- **Поле ввода** — Отправка текста в stdin работающего инструмента (для интерактивных запросов)
- **Кроссплатформенная прокрутка** — Нативная на macOS (pyobjc), Button-4/5 на Linux, MouseWheel на Windows

### Схема интерфейса

```
┌──────────────────────────────────────────────────┐
│  NOC Toolkit v0.6.1    [⚙ Config] [↻ Reload]    │
├────────┬─────────────────────────────────────────┤
│        │  ┌─ Параметры ───────────────────────┐  │
│ PD-Jira│  │  ☑ --dry-run   ☐ --verbose        │  │
│ Job Ext│  │  ◉ --snooze  ○ --update            │  │
│ PD Mon │  │  [Запуск ▶]  [Стоп ■]  ● Готов     │  │
│ PD Merg│  ├────────────────────────── сплиттер──┤  │
│ Freshns│  │  Вывод консоли...                   │  │
│ NOC Rpt│  │  > Запуск pd-sync...                │  │
│ GSheet │  │  > Найдено 12 инцидентов            │  │
│ PD Esc │  │                                     │  │
│ PD Res │  │  [поле ввода___________] [Отпр.]    │  │
│ Tkt Wch│  │  [Очистить]     Вывод: 42 строк     │  │
└────────┴─────────────────────────────────────────┘
```

---

## 🎮 Использование CLI

При запуске CLI (`noc-toolkit.py`) вы увидите интерактивное меню:

```
╔════════════════════════════════════════════════════════╗
║                                                        ║
║              NOC Toolkit v0.6.1                        ║
║                                                        ║
║         Unified NOC Operations Toolkit                 ║
║                                                        ║
╚════════════════════════════════════════════════════════╝

========================================================
Available Tools:
========================================================
  1. [✓] PD Sync
      Sync PagerDuty incidents with Jira issues

  2. [✓] PD Jobs
      Extract and analyze PagerDuty on-call schedules

  3. [✓] PD Monitor
      Monitor and auto-acknowledge triggered incidents

  4. [✓] PD Merge
      Find and merge related PagerDuty incidents by job name

  5. [✓] Freshness
      DACSCAN data freshness report with granular table checks

  6. [✓] Shift Report
      Sync Jira statuses into shift report (Google Sheets / Excel)

  7. [✓] PD Escalate
      Link DRGN→DSSD, transition to Escalated, post PD note

  8. [✓] PD Resolve
      Auto-resolve PD incidents where Airflow jobs recovered

  9. [✓] Ticket Watch
      Monitor escalation tickets for unassigned/stale states

--------------------------------------------------------
  0. Exit
========================================================

Select tool [0-9]:
```

### Навигация в меню

- Введите **номер** инструмента, который хотите запустить (например, `1` для PD Sync)
- Введите **0** для выхода из toolkit
- Введите **gui** для запуска GUI из CLI меню
- Нажмите **Ctrl+C** в любой момент для прерывания и возврата в меню

### Индикаторы статуса инструментов

- **[✓]** - Инструмент доступен и готов к использованию
- **[✗]** - Скрипт инструмента не найден (проверьте конфигурацию)

### Индикаторы статуса конфигурации

- **✓ Config: Environment loaded from: .env** - Конфигурация успешно загружена
- **⚠️ Config: No .env file found** - Файл .env не найден, нужно настроить

---

## 🔧 Доступные инструменты

### 1. PagerDuty-Jira Tool

**Назначение:** Синхронизация инцидентов PagerDuty с задачами Jira

**Возможности:**
- Получение инцидентов PagerDuty по заданным фильтрам
- Автоматическое обнаружение Jira тикетов из заголовков и комментариев инцидентов
- Отслеживание Jira статусов и публикация комментариев с обновлением статуса
- Режим авто-snooze с настраиваемой длительностью
- Автоопределение ключевых слов «ignore»/«disabled» — короткий комментарий и snooze
- 12-часовая защита от дублирования комментариев
- Прогресс-бар с оценкой времени

**Конфигурация:** Использует общий `.env` из корня toolkit (PAGERDUTY_API_TOKEN + Jira credentials)

**Необходимые переменные в `.env`:**
- `PAGERDUTY_API_TOKEN`
- `JIRA_SERVER_URL`
- `JIRA_PERSONAL_ACCESS_TOKEN` (для Jira Server/Data Center)
  ИЛИ
- `JIRA_EMAIL` + `JIRA_API_TOKEN` (для Jira Cloud)

---

### 2. PagerDuty Job Extractor

**Назначение:** Извлечение и анализ графиков дежурств PagerDuty

**Возможности:**
- Получение расписаний PagerDuty
- Извлечение данных о ротации дежурств
- Генерация отчетов о назначенных задачах
- Экспорт данных в различных форматах (CSV, JSON)

**Конфигурация:** См. [tools/pagerduty-job-extractor/README.md](tools/pagerduty-job-extractor/README.md)

**Необходимые переменные в `.env`:**
- `PAGERDUTY_API_TOKEN`

---

### 3. PagerDuty Monitor

**Назначение:** Автоматически подтверждает triggered инциденты и оставляет человекоподобные комментарии

**Возможности:**
- Авто-подтверждение triggered инцидентов, назначенных текущему пользователю
- Рандомизированные фразы комментариев (13 обычных + 10 с опечатками) для имитации живого инженера
- 20% вероятность опечатки, 50% вероятность маленькой буквы для естественности
- Тихий acknowledge (без комментария) для «Missing» инцидентов load-status (AUS & NZL, MSP Export, CANADA, Central, East, International, UK)
- **Фоновый режим** — мониторинг работает пока вы используете другие инструменты, просмотр вывода по запросу, авто-остановка при выходе
- Определение ранее оставленных авто-комментов для предотвращения дублей
- Непрерывный мониторинг с настраиваемой длительностью и интервалом проверки
- Режим dry-run для безопасного тестирования

**Конфигурация:** Использует общий `.env` из корня toolkit (PAGERDUTY_API_TOKEN)

**Быстрая настройка:**
```bash
cd tools/pd-monitor
# API токен используется из общего .env noc-toolkit
python3 pd_monitor.py --dry-run --verbose  # Сначала тест
```

**Настройка Cron (рекомендуется):**
```bash
crontab -e
# Добавить: */10 * * * * cd /Users/master/pd-monitor && python3 pd_monitor.py >> /tmp/pd-monitor.log 2>&1
```

**Необходимые переменные в `.env`:**
- `PAGERDUTY_API_TOKEN`
- `MONITOR_ACKNOWLEDGE_THRESHOLD_HOURS` (опционально, по умолчанию: 4.0)
- `MONITOR_COMMENT_PATTERN` (опционально, по умолчанию: "working on it")
- `MONITOR_MAX_AUTO_REFRESHES` (опционально, по умолчанию: 3)

---

### 4. PagerDuty Incident Merge

**Назначение:** Поиск и слияние связанных инцидентов PagerDuty, имеющих одну и ту же первопричину (одно имя джобы/DAG)

**Возможности:**
- Автоматическая группировка инцидентов по нормализованному имени джобы
- Четыре сценария слияния: same-day (A), cross-date с проверкой через Jira (B), массовый сбой (C), RDS exports «failed to start» (D)
- Детерминированный выбор целевого инцидента: реальные комментарии > приоритет алерта (Databricks > Monitor > AirFlow) > самый ранний
- Интерактивное подтверждение по группам и по отдельным инцидентам
- Сохранение пропущенных инцидентов между запусками, интерактивная очистка при старте
- Детальная таблица слияния с PD-ссылками, именами инцидентов и возрастом dd:hh:mm
- Режим dry-run для безопасного предпросмотра

**Быстрая настройка:**
```bash
python3 tools/pd-merge/pd_merge.py --dry-run    # Предпросмотр
python3 tools/pd-merge/pd_merge.py               # Запуск
```

**Опции CLI:**
- `--dry-run, -n` — Симуляция без изменений в API
- `--verbose, -v` — Подробный вывод для отладки
- `--clear-skips` — Очистить список пропущенных инцидентов
- `--show-skips` — Показать текущие пропущенные инциденты

**Необходимые переменные в `.env`:**
- `PAGERDUTY_API_TOKEN`
- `JIRA_SERVER_URL` (опционально, для Сценария B)
- `JIRA_PERSONAL_ACCESS_TOKEN` (опционально, для Сценария B)

---

### 5. Data Freshness Checker

**Назначение:** Автоматизация ежедневного отчёта DACSCAN Data Freshness через Databricks SQL

**Возможности:**
- Основной отчёт на 15 таблиц (DACSCAN, AGG, AUDIT, SUMMARY, BI-LOADER)
- Автоматические гранулярные проверки для задержанных таблиц (host-level для DACSCAN, max(update_ts) для агрегатных)
- Обработка известной проблемы SALES_ORD_EVENT_OPT (DSSD-29069 — fallback на update_ts)
- Обратный отсчёт SLA (дедлайн 5:30 PM UTC)
- HTML-отчёт с цветовой кодировкой — открывается в браузере для скриншотов в Slack
- Подключение через Databricks SQL Statement Execution REST API (без тяжёлого SDK)

**Быстрая настройка:**
```bash
python3 tools/data-freshness/data_freshness.py --dry-run    # Предпросмотр SQL
python3 tools/data-freshness/data_freshness.py               # Запуск отчёта
python3 tools/data-freshness/data_freshness.py --report      # Запуск + HTML отчёт
```

**Опции CLI:**
- `--report, -r` — Генерация HTML-отчёта и открытие в браузере
- `--check-all` — Гранулярные проверки для ВСЕХ таблиц (не только задержанных)
- `--dry-run, -n` — Показать SQL запросы без выполнения
- `--verbose, -v` — Показать детали API вызовов
- `--format csv/json` — Альтернативные форматы вывода

**Необходимые переменные в `.env`:**
- `DATABRICKS_HOST`
- `DATABRICKS_TOKEN`
- `DATABRICKS_WAREHOUSE_ID`

---

### 6. NOC Report Assistant

**Назначение:** Автоматизация смены шифта, синхронизация Jira статусов и добавление строк с тикетами в End-of-Shift Excel отчёт

**Возможности:**
- **Start shift** — копирование всех тикетов с предыдущей смены, обновление даты, синхронизация Jira статусов
- **End shift (SYNC)** — обновление Jira статусов (колонка E) для существующих тикетов
- **Add row** — вставка нового тикета в секцию "Things to monitor" с Jira + Slack ссылками
- Автоопределение секций ("from previous shifts", "Things to monitor", "Permalinks")
- Вставка/удаление строк при разном количестве тикетов между сменами
- Обработка перехода через месяц (напр. 31 марта → 1 апреля)
- Автоопределение Jira и Slack ссылок в любом порядке вставки
- Сохранение всего форматирования Excel, объединений ячеек и гиперссылок
- Работает с обоими листами: Night-Shift-NEW и Day-Shift-NEW

**Быстрая настройка:**
```bash
python3 tools/noc-report-assistant/noc_report_assistant.py --dry-run    # Предпросмотр
python3 tools/noc-report-assistant/noc_report_assistant.py               # Запуск
```

**Опции CLI:**
- `--dry-run, -n` — Показать изменения без сохранения
- `--verbose, -v` — Показать детали API вызовов
- `--file PATH` — Путь к Excel файлу (по умолчанию: `~/Downloads/NOC endshift report.xlsx`)

**Необходимые переменные в `.env`:**
- `JIRA_SERVER_URL`
- `JIRA_PERSONAL_ACCESS_TOKEN`
- `NOC_REPORT_PATH` (опционально)

---

### 7. PD Escalation Tool

**Назначение:** Автоматизация workflow эскалации после создания DSSD — линковка DRGN→DSSD в Jira, переход DRGN в статус Escalated, PD заметка, Slack шаблон

**Возможности:**
- Автоопределение DRGN тикета через поле Jira интеграции PD (`external_references`)
- Фоллбэк: поиск DRGN-\d+ в заметках PD инцидента
- Если DRGN не найден: показывает ссылку на PD инцидент с инструкцией нажать кнопку "Create Jira Issue"
- Создание Jira-ссылки: DRGN «is blocked by» DSSD
- Перевод DRGN в статус «Escalated»
- PD заметка с деталями эскалации и ссылкой на Jira
- Slack шаблон для #cds-ops-24x7-int с инструкцией по гиперссылке
- Режим dry-run для безопасного тестирования

**Быстрая настройка:**
```bash
python3 tools/pd-escalate/pd_escalate.py --pd Q33L5GALLQ3ESB --dssd DSSD-29386 --dry-run    # Предпросмотр
python3 tools/pd-escalate/pd_escalate.py --pd Q33L5GALLQ3ESB --dssd DSSD-29386               # Запуск
```

**Опции CLI:**
- `--pd` — ID инцидента PagerDuty или URL (обязательно)
- `--dssd` — Ключ тикета DSSD, напр. DSSD-29386 (обязательно)
- `--drgn` — Ключ тикета DRGN (опционально, авто-определяется)
- `--dry-run, -n` — Симуляция без изменений в API

**Необходимые переменные в `.env`:**
- `PAGERDUTY_API_TOKEN`
- `JIRA_SERVER_URL`
- `JIRA_PERSONAL_ACCESS_TOKEN`

---

## 🔐 Безопасность

### Важные замечания по безопасности

- **Никогда не коммитьте файл `.env`** - Он содержит конфиденциальные учетные данные
- **Храните API токены в безопасности** - Сохраняйте их только в переменных окружения или файле `.env`
- **Используйте токены только для чтения, когда возможно** - Ограничьте права токенов до минимально необходимых
- **Ограничьте доступ** - Только авторизованный персонал должен иметь доступ к toolkit

### Права доступа к файлам

Убедитесь, что конфигурационные файлы имеют соответствующие права:

```bash
chmod 600 .env  # Чтение/запись только для владельца
```

---

## 🔧 Централизованная конфигурация

### Преимущества единого .env файла

✅ **Настройте один раз, используйте везде** - Все инструменты используют один файл .env
✅ **Нет дублирования** - Не нужно копировать токены в несколько мест
✅ **Проще управлять** - Обновите токен в одном месте
✅ **Меньше ошибок** - Снижается риск использования устаревших токенов

### Структура конфигурации

```
noc-toolkit/
├── .env                    # ← Единый файл конфигурации (создайте из .env.example)
├── .env.example            # ← Шаблон со всеми доступными переменными
└── tools/
    ├── pd-jira-tool/      # Использует общий .env из корня
    └── pagerduty-job-extractor/  # Использует общий .env из корня
```

### Как это работает

1. При запуске `noc-toolkit.py` автоматически загружает `.env` из корневой директории
2. Все переменные окружения становятся доступны всем запускаемым инструментам
3. Инструменты автоматически получают доступ к переменным (PAGERDUTY_API_TOKEN, JIRA_*, и т.д.)
4. Не нужно настраивать `.env` в каждой директории инструмента

### Полный пример .env файла

```bash
# ============================================================================
# PagerDuty API Configuration
# ============================================================================
PAGERDUTY_API_TOKEN=your_pagerduty_api_token_here

# ============================================================================
# Jira Configuration (для Jira Server/Data Center)
# ============================================================================
JIRA_SERVER_URL=https://your-jira-server.com
JIRA_PERSONAL_ACCESS_TOKEN=your_jira_personal_access_token_here

# ============================================================================
# Опциональные настройки
# ============================================================================
# SNOOZE_DURATION_HOURS=6.0
# LOG_LEVEL=INFO
# OUTPUT_DIR=./output
```

---

## 🛠️ Устранение неполадок

### Распространенные проблемы

#### Ошибка "Module not found"

**Проблема:** Python зависимости не установлены

**Решение:**
```bash
pip3 install -r requirements.txt
```

#### "Permission denied" при запуске toolkit

**Проблема:** Скрипт не имеет прав на выполнение

**Решение:**
```bash
chmod +x noc-toolkit.py
```

#### Инструмент показывает [✗] в меню

**Проблема:** Скрипт инструмента не найден или символическая ссылка повреждена

**Решение:**
```bash
# Проверьте директорию tools
ls -la tools/

# Пересоздайте символические ссылки при необходимости
ln -sf /Users/master/pd-jira-tool tools/pd-jira-tool
ln -sf /Users/master/pagerduty-job-extractor tools/pagerduty-job-extractor
```

#### Ошибки аутентификации API

**Проблема:** Неверные или отсутствующие учетные данные

**Решение:**
1. Убедитесь, что файл `.env` существует в корне noc-toolkit
2. Проверьте, что API токены действительны и не истекли
3. Убедитесь, что токены имеют необходимые права доступа
4. Проверьте правильность URL (JIRA_SERVER_URL)

#### Предупреждение "⚠️ Config: No .env file found"

**Проблема:** Файл `.env` не создан

**Решение:**
```bash
cd /Users/master/noc-toolkit
cp .env.example .env
nano .env  # Заполните ваши учетные данные
```

---

## 📚 Документация

- **[README.md](README.md)** - Английская версия руководства
- **[docs/PROJECT_DOCS.md](docs/PROJECT_DOCS.md)** - Полная техническая документация
- **[docs/PLAN.md](docs/PLAN.md)** - План разработки и отслеживание прогресса
- **[docs/CONTEXT.md](docs/CONTEXT.md)** - Журнал обсуждений и решений
- **Документация инструментов** - См. индивидуальные директории инструментов

---

## 🤝 Добавление новых инструментов

Хотите добавить новый инструмент в toolkit?

### Шаг 1: Добавьте инструмент в директорию tools/

```bash
# Вариант А: Скопируйте инструмент
cp -r /путь/к/вашему/инструменту tools/мой-новый-инструмент

# Вариант Б: Создайте символическую ссылку (рекомендуется)
ln -s /путь/к/вашему/инструменту tools/мой-новый-инструмент
```

### Шаг 2: Зарегистрируйте инструмент в noc-toolkit.py

Отредактируйте файл `noc-toolkit.py` и добавьте определение инструмента в метод `_load_tools()` (строки 84-95):

```python
ToolDefinition(
    tool_id="мой-новый-инструмент",
    name="Мой Новый Инструмент",
    description="Описание того, что делает инструмент",
    script_path="tools/мой-новый-инструмент/main.py",
    enabled=True
),
```

### Шаг 3: Добавьте необходимые переменные окружения

Если ваш инструмент требует дополнительных переменных окружения, добавьте их в `.env.example`:

```bash
# Мой Новый Инструмент
# ----------------------------------------------------------------------------
# Описание переменной
MY_TOOL_API_KEY=ваш_ключ_здесь
MY_TOOL_SETTING=значение
```

### Шаг 4: Обновите зависимости

Если инструмент требует дополнительных Python пакетов:

```bash
echo "ваш-пакет>=1.0.0" >> requirements.txt
pip3 install -r requirements.txt
```

### Шаг 5: Протестируйте

```bash
python3 noc-toolkit.py
# Выберите ваш новый инструмент из меню
```

---

## 📊 Структура проекта

```
noc-toolkit/
├── noc-toolkit.py              # CLI точка входа (меню)
├── noc_toolkit_gui.py          # GUI точка входа (customtkinter)
├── config.yaml                 # Общая конфигурация (API токены, URL)
├── config.yaml.example         # Шаблон конфигурации
├── tools/                      # Все инструменты
│   ├── common/                # Общие утилиты (noc_utils.py)
│   ├── pd-sync/               # PD-Jira синхронизация
│   ├── pd-jobs/               # Извлечение джоб из PD
│   ├── pd-monitor/            # Авто-подтверждение инцидентов
│   ├── pd-merge/              # Слияние инцидентов
│   ├── freshness/             # DACSCAN отчёт свежести данных
│   ├── shift-report/          # Shift report (Google Sheets / Excel)
│   ├── pd-escalate/           # Автоматизация эскалации DRGN→DSSD
│   ├── pd-resolve/            # Авто-resolve восстановленных инцидентов
│   └── ticket-watch/          # Мониторинг эскалационных тикетов
├── tests/                      # Юнит-тесты (1275 тестов, 94% покрытие)
│   ├── test_noc_toolkit.py    # Тесты CLI лаунчера
│   ├── test_noc_toolkit_gui.py # Тесты логики GUI (91 тест)
│   └── test_*.py              # Тесты по каждому инструменту
├── logs/                       # Лог-файлы по инструментам (gitignored)
├── docs/                       # Документация
│   ├── PROJECT_DOCS.md        # Архитектурная документация
│   └── PLAN.md                # План разработки
├── setup.cfg                   # Конфигурация pytest + coverage
├── requirements.txt            # Python зависимости
├── README.md                   # Английская документация
└── README_RU.md                # Эта документация
```

---

## 🔄 История версий

### GUI v0.1.0 (2026-04-07)

- ✅ **Новое:** Десктоп GUI на customtkinter (`noc_toolkit_gui.py`)
- ✅ Тёмная тема с вертикальными вкладками-сайдбаром (10 инструментов)
- ✅ Параллельный запуск — одновременное выполнение нескольких инструментов
- ✅ Лог-файлы по инструментам в директории `logs/` с таймстемпами
- ✅ Кнопки Запуск/Стоп и индикаторы статуса по каждому инструменту
- ✅ Панель конфигурации через кнопку в шапке (редактирование `config.yaml`)
- ✅ Перетаскиваемый сплиттер PanedWindow между параметрами и консолью
- ✅ Поле ввода stdin для интерактивных запросов инструментов
- ✅ Кроссплатформенная прокрутка: нативная macOS (pyobjc), Linux (Button-4/5), Windows (MouseWheel)
- ✅ 91 юнит-тест (pytest, на моках без Tk дисплея)

### NOC Toolkit v0.6.1 (2026-03-21)

- ✅ Рефакторинг общих утилит в `tools/common/noc_utils.py`
- ✅ Все инструменты мигрированы на `load_env()`, `require_env()`, `new_pd_client()`, `new_jira_client()`
- ✅ Лаунчер обновлён: `PYTHONPATH` включает `tools/common/`
- ✅ Всего тестов: 1275 по всем модулям (94% покрытие)

### ticket-watch v0.1.0 (2026-03-20)

- ✅ **Новый инструмент:** Мониторинг эскалационных тикетов на неназначенные/застойные состояния
- ✅ JQL поиск по настраиваемому списку репортеров и проекту
- ✅ Пинг назначенных на застойные тикеты (3+ дней без комментария)
- ✅ Определение повторных пингов с последним ответом назначенного
- ✅ `--dry-run`, `--no-comment`, `--project` CLI опции

### pd-resolve v0.1.2 (2026-03-17)

- ✅ **Баг-фикс:** Проверка восстановления теперь требует 2 последовательных успешных запуска вместо всех 15

### pd-monitor v0.1.4 (2026-03-12)

- ✅ **Баг-фикс:** множество `processed_incidents` никогда не очищалось между циклами проверки — после авто-отмены ack PagerDuty (~30 мин) повторно triggered инциденты навсегда пропускались как "already processed"
- ✅ Кэширование email пользователя при инициализации — убран лишний `GET /users/{id}` при каждом acknowledge
- ✅ Убран `sys.exit(1)` из `_get_current_user_id()` — бросает `RuntimeError`
- ✅ 97 юнит-тестов (pytest)

### pd-jira-tool v0.3.2 (2026-03-12)

- ✅ Выделены хелперы `_parse_iso_dt()` и `_is_assigned_to_user()` — устраняют дублирование ISO-парсинга и фильтрации по пользователю
- ✅ Убран `sys.exit(1)` из `check_incidents()` и `process_and_update_incidents()` — исключения пробрасываются вызывающему коду
- ✅ 96 юнит-тестов (pytest)

### pagerduty-job-extractor v0.1.1 (2026-03-12)

- ✅ Убран `sys.exit(1)` из бизнес-логики — исключения пробрасываются вызывающему коду
- ✅ Исправлен тип `any` → `Any`, убраны промежуточные `list()` на итераторах
- ✅ 39 юнит-тестов (pytest)

### pd-merge v0.2.4 (2026-03-12)

- ✅ Выделен хелпер `_parse_iso_dt()` — устраняет 7 дублирующихся парсингов ISO datetime
- ✅ 70 юнит-тестов (pytest)

### data-freshness v0.1.1 (2026-03-12)

- ✅ Выделен хелпер `_is_fresh_date()` — устраняет 5 дублирующихся проверок даты свежести
- ✅ Исправлен импорт `timedelta` (был внутри тела функции)
- ✅ 48 юнит-тестов (pytest)

### pd-escalate v0.1.1 (2026-03-11)

- ✅ Рефакторинг: `JIRA_BASE_URL` из глобальной переменной в атрибут экземпляра, удалён `PD_BASE_URL`
- ✅ `sys.exit(1)` в `run()` заменён на `RuntimeError` для чистой обработки ошибок
- ✅ 37 юнит-тестов (pytest)

### Версия 0.6.0 (2026-03-07)

- ✅ Интегрирован PD Escalation Tool (pd-escalate v0.1.0)
- ✅ Автоматизация эскалации: линковка DRGN→DSSD, переход в Escalated, PD заметка, Slack шаблон
- ✅ Автоопределение DRGN через PD Jira integration field (`external_references`)
- ✅ Зарегистрирован как инструмент #7 в меню noc-toolkit

### noc-report-assistant v0.1.5 (2026-03-11)

- ✅ Рефакторинг: единый `_scan_layout()` во всех методах, удалён `STOP_MARKERS`
- ✅ Один проход I/O в `start_shift()` (было двойное чтение/запись), `_scan_layout()` вызовов: 6→2
- ✅ Выделены подметоды: `_restructure_from_prev()`, `_reset_ttm()`, `_repair_permalinks()`
- ✅ 46 юнит-тестов (pytest)

### noc-report-assistant v0.1.4 (2026-03-11)

- ✅ Фикс: обработка отсутствующего заголовка "from the previous shifts" (фоллбэк на строку 8)

### noc-report-assistant v0.1.3 (2026-03-09)

- ✅ Автоматизация смены шифта: "Start shift" копирует тикеты с предыдущей смены, обновляет дату, синкает
- ✅ Меню переупорядочено: 1=Start shift, 2=End shift (SYNC), 3=Add row
- ✅ Обработка перехода через месяц (напр. 31 марта → 1 апреля)

### noc-report-assistant v0.1.2 (2026-03-09)

- ✅ Баг-фикс: синк теперь обрабатывает тикеты внутри секции "Things to monitor" (ранее пропускались)

### noc-report-assistant v0.1.1 (2026-03-07)

- ✅ Исправлен цвет гиперссылок: явный Jira-blue (#0052CC) с подчёркиванием для ссылок на тикеты и Slack

### Версия 0.5.0 (2026-03-03)

- ✅ Интегрирован NOC Report Assistant (noc-report-assistant v0.1.0)
- ✅ Синхронизация Jira статусов для существующих тикетов в End-of-Shift Excel отчёте
- ✅ Добавление строк с тикетами в секцию "Things to monitor" с Jira + Slack ссылками
- ✅ Надёжная обработка openpyxl: объединения ячеек, гиперссылки, форматирование

### Версия 0.4.0 (2026-02-27)

- ✅ Интегрирован Data Freshness Checker (data-freshness v0.1.0)
- ✅ Автоматизирован DACSCAN отчёт на 15 таблиц через Databricks SQL REST API
- ✅ Гранулярные проверки с подсчётом хостов и верификацией update_ts
- ✅ HTML-отчёт с цветовой кодировкой для Slack

### Версия 0.3.0 (2026-02-26)

- ✅ Интегрирован PagerDuty Incident Merge (pd-merge v0.2.0)
- ✅ Три сценария слияния: same-day, cross-date с Jira, массовый сбой
- ✅ Интерактивный выбор отдельных инцидентов и сохранение пропусков

### Версия 0.2.0 (2026-02-25)

- ✅ Исправления PyInstaller EXE — инструменты работают внутри скомпилированного бинарника
- ✅ Диагностический debug лог при каждом запуске

### Версия 0.1.0 (2026-02-22)

**Первый релиз**

- ✅ Интерфейс с меню
- ✅ Интегрирован PagerDuty-Jira Tool, Job Extractor, PD Monitor
- ✅ Централизованная конфигурация через единый .env
- ✅ Проверка здоровья инструментов

---

## 📝 Лицензия

Внутренний инструмент только для организационного использования.

---

## 💬 Поддержка

При возникновении вопросов или проблем:

1. Проверьте раздел устранения неполадок выше
2. Изучите документацию конкретного инструмента
3. Просмотрите [PROJECT_DOCS.md](docs/PROJECT_DOCS.md) для технических деталей
4. Проверьте [CONTEXT.md](docs/CONTEXT.md) для истории решений
5. Обратитесь к команде NOC

---

## 🎯 Будущие улучшения

Планируемые функции для будущих версий:

- 📈 Статистика использования
- 🔔 Интеграции с уведомлениями (Slack, email)
- 🏗️ Сборка PyInstaller GUI для распространения на Windows/macOS

**Реализовано:**
- ~~🎨 Цветной вывод в терминале~~ → GUI с тёмной темой
- ~~📊 Встроенная система логирования~~ → Лог-файлы по инструментам в `logs/`
- ~~⚙️ Мастер настройки~~ → GUI панель конфигурации
- ~~🔍 Функция поиска инструментов~~ → GUI сайдбар со всеми инструментами

См. [PLAN.md](docs/PLAN.md) для полного плана развития.

---

## 🙏 Авторы

Создано командой NOC для упрощения операционных задач.

---

**Сделано с ❤️ для команды NOC**

**Версия документа:** 0.6.1
**Последнее обновление:** 2026-04-07
