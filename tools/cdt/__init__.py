"""CDT (Core Data Tools / Teal Triangle) read-only API client.

Wraps the Control Panel API at controlpanel.prd2971.prod9.us-east-1.tktm.io
using a Bearer token from CDT UI: Platform → Auth Tokens → Create.
"""

VERSION = "0.1.0"
